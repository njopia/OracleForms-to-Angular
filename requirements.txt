# Oracle Forms to XML Converter Requirements
# Python 3.8+ required

# Tkinter is included with Python standard library on most platforms
# For Linux systems, you may need to install it separately:
# Ubuntu/Debian: sudo apt-get install python3-tk
# Fedora/RHEL: sudo dnf install python3-tkinter
# Arch: sudo pacman -S tk

# No additional pip packages required for the base functionality
# All dependencies are part of Python standard library:
# - tkinter (GUI)
# - subprocess (process execution)
# - json (configuration management)
# - pathlib (file path handling)
